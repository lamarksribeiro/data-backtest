# Lab operations

Scripts de operação (SSH, Docker, filas remotas). Código de produto e CLI local ficam em `labs/cli/` e `labs/shared/`.

- `brutus/` — execução no servidor Brutus / container Coolify
